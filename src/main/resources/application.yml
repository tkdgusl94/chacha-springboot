server:
  port: 9000

database: hsqldb

spring:
  datasource:
    schema: classpath*:db/${database}/schema.sql
    data: classpath*:db/${database}/data.sql

  jpa:
    show_spl: true
    hibernate:
      ddl-auto: none
      properties:
        hibernate:
          format_sql: true

#  datasource:
#    url: jdbc:h2:mem:test #
##    url: jdbc:h2:tcp://localhost/~/h2_database/chacha
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

  h2:
    console:
      enabled: true

logging:
  level:
    org:
      springframework.web: debug